# zulfr.com
hosting dais10 on zulfr website

```code
zulfr/
├── index.html        ← clean HTML only, links to CSS and JS
├── css/
│   └── style.css     ← all styles, tokens, and media queries
└── js/
    └── script.js     ← tab switching, paper expand, model switcher, filter logic
```
